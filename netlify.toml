[build]
  command = "pnpm exec prisma generate && pnpm exec prisma migrate deploy && pnpm run db:seed && pnpm run netlify:build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24.18.0"
  PNPM_FLAGS = "--frozen-lockfile"

[[plugins]]
  package = "@netlify/plugin-nextjs"
